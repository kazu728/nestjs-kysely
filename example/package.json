{
  "name": "example",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "prebuild": "rimraf dist",
    "postinstall": "cd .. && npm install && npm run build",
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:debug": "nest start --debug --watch",
    "start:prod": "node dist/main",
    "test:cjs": "cd cjs && npm install && npm test",
    "test:esm": "cd esm && npm install && npm test",
    "test:modules": "npm run test:cjs && npm run test:esm"
  },
  "dependencies": {
    "@nestjs/common": "^11.2.1",
    "@nestjs/core": "^11.2.1",
    "@nestjs/platform-express": "^11.2.1",
    "kysely": "^0.29.5",
    "mysql2": "^3.23.3",
    "nestjs-kysely": "file:..",
    "reflect-metadata": "^0.2.2",
    "rimraf": "^6.0.1",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.5",
    "@nestjs/schematics": "^11.0.2",
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.10",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.2"
  }
}
